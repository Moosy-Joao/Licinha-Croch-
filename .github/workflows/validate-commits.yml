name: Validate Commit Messages

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  commitlint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Obtém todo o histórico

      - name: Get commit messages
        run: |
          git log --format="%s" origin/main..HEAD > commits.txt

      - name: Validate commit messages
        run: |
          PATTERN="^(feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert|merge)(\\(.+\\))?: .{1,100}$"
          while IFS= read -r line; do
            if ! [[ $line =~ $PATTERN ]]; then
              echo "❌ Commit inválido: '$line'"
              exit 1
            else
              echo "✅ Commit válido: '$line'"
            fi
          done < commits.txt
